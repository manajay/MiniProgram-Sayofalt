<!--pages/user/user.wxml-->
<view class='container'>
  <view class="header-view"></view>
  <l-list class="list-item" wx:for="{{list_data}}" wx:key="index" key="{{item.id}}" title="{{item.title}}" desc="{{item.desc}}" icon="{{item.icon}}" />
</view>